import 'package:flutter/material.dart';

class ScreenViewVerse extends StatelessWidget{
  const ScreenViewVerse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }

}